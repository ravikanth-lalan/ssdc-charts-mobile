import React from 'react';
import WebViewWrapper from '../components/webview';

const NivoCharts = _ => (
  <WebViewWrapper uri="https://ssdc-charts.herokuapp.com/nivo" />
);

export default NivoCharts;
