import React from 'react';
import WebViewWrapper from '../components/webview';

const VictoryCharts = _ => (
  <WebViewWrapper uri="https://ssdc-charts.herokuapp.com/victory" />
);

export default VictoryCharts;
